//create a bug that outputs an unexpected result
// function addNums(a, b){
//   console.log(a + b);
// }
// addNums("1", 2);// output: 12; this is due to the concatination properties of strings
//
// console.log("Still Running");// Shows that a but has occured but the program still finishes running
//
// console.log(a + b);// ouputs an error because a is not defined as a variable
// console.log("Still Running");//does not run due to an error being thrown earlier in the program

// console.log("hello);// creates a syntax error due to the string missing an ending quotation 

(5).pop; // creates a type error because pop is a method of arrays not of integers
